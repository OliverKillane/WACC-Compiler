# Correctness of find_nth_char_arr()

# Output:
# -1
# -1
# 0
# -1
# -1
# -1
# 2
# 4
# -1
# -1
# 9
# -1
# 1
# -1
# -1


mod ../../../../../stdlib/array_utils.wacc;

begin
	char[] arr = [] ;
	int index = call find_nth_char_arr('c', 2, arr) ;
	println index ;
	arr = ['Z'] ;
	index = call find_nth_char_arr(']', 2, arr) ;
	println index ;
	arr = ['Y', ']'] ;
	index = call find_nth_char_arr('Y', 1, arr) ;
	println index ;
	arr = [']', 'Y', '^'] ;
	index = call find_nth_char_arr('[', 3, arr) ;
	println index ;
	arr = ['Z', '`', '^', 'b'] ;
	index = call find_nth_char_arr('`', 2, arr) ;
	println index ;
	arr = ['[', 'Y', '^', 'X', 'Y'] ;
	index = call find_nth_char_arr('c', 2, arr) ;
	println index ;
	arr = ['[', 'a', 'Z', '[', 'Z', '^'] ;
	index = call find_nth_char_arr('Z', 1, arr) ;
	println index ;
	arr = ['Y', 'c', 'Y', 'X', '_', 'Z', ']'] ;
	index = call find_nth_char_arr('_', 1, arr) ;
	println index ;
	arr = ['a', 'Z', '_', '^', '_', '_', 'X', 'c'] ;
	index = call find_nth_char_arr('`', 3, arr) ;
	println index ;
	arr = ['b', '`', '_', '^', 'a', 'X', 'Z', '_', '`'] ;
	index = call find_nth_char_arr(']', 3, arr) ;
	println index ;
	arr = ['a', 'Z', 'a', 'a', 'c', ']', ']', ']', 'X', '['] ;
	index = call find_nth_char_arr('[', 1, arr) ;
	println index ;
	arr = ['b', '`', '[', '_', 'c', 'Y', 'X', '^', '`', 'b', '['] ;
	index = call find_nth_char_arr('[', 3, arr) ;
	println index ;
	arr = ['c', 'X', '`', '^', 'Y', 'Y', 'Y', '`', 'a', '_', '_', ']'] ;
	index = call find_nth_char_arr('X', 1, arr) ;
	println index ;
	arr = ['b', '[', ']', 'b', '_', '^', 'a', 'c', '`', 'X', 'a', 'c', '['] ;
	index = call find_nth_char_arr('Z', 1, arr) ;
	println index ;
	arr = ['Z', 'b', 'Z', '_', '[', 'c', '`', '[', ']', '_', 'c', '[', '^', '['] ;
	index = call find_nth_char_arr('c', 3, arr) ;
	println index
end