# Correctness of find_first_char_arr()

# Output:
# -1
# -1
# -1
# -1
# -1
# -1
# -1
# 3
# 4
# 7
# 1
# 4
# 9
# 2
# 8


mod ../../../../../stdlib/array_utils.wacc;

begin
	char[] arr = [] ;
	int index = call find_first_char_arr('[', arr) ;
	println index ;
	arr = ['`'] ;
	index = call find_first_char_arr('^', arr) ;
	println index ;
	arr = ['c', '^'] ;
	index = call find_first_char_arr('X', arr) ;
	println index ;
	arr = ['c', '[', 'c'] ;
	index = call find_first_char_arr('`', arr) ;
	println index ;
	arr = ['`', '_', 'c', '_'] ;
	index = call find_first_char_arr('Y', arr) ;
	println index ;
	arr = ['_', ']', '[', '_', 'a'] ;
	index = call find_first_char_arr('c', arr) ;
	println index ;
	arr = ['c', ']', '^', ']', 'X', 'Z'] ;
	index = call find_first_char_arr('Y', arr) ;
	println index ;
	arr = ['_', 'a', 'X', '^', '_', 'b', 'b'] ;
	index = call find_first_char_arr('^', arr) ;
	println index ;
	arr = ['[', '^', '`', 'c', ']', '^', '^', 'X'] ;
	index = call find_first_char_arr(']', arr) ;
	println index ;
	arr = ['^', '[', 'c', 'a', 'Y', '_', '_', ']', ']'] ;
	index = call find_first_char_arr(']', arr) ;
	println index ;
	arr = ['`', ']', '^', 'Z', 'a', '[', '`', '_', 'c', 'Y'] ;
	index = call find_first_char_arr(']', arr) ;
	println index ;
	arr = ['_', '_', 'X', 'b', '[', 'b', 'Y', '_', '`', '[', ']'] ;
	index = call find_first_char_arr('[', arr) ;
	println index ;
	arr = ['X', 'Z', 'a', 'c', 'X', '^', 'c', '`', 'a', '[', ']', '^'] ;
	index = call find_first_char_arr('[', arr) ;
	println index ;
	arr = ['[', 'X', ']', 'a', '^', ']', 'Y', 'Y', '`', 'X', 'Z', 'Y', 'X'] ;
	index = call find_first_char_arr(']', arr) ;
	println index ;
	arr = ['_', '_', 'X', '[', 'Z', '[', 'a', '_', 'c', ']', 'X', ']', 'c', '['] ;
	index = call find_first_char_arr('c', arr) ;
	println index
end