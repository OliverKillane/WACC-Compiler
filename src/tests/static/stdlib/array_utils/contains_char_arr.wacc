# Correctness of contains_char_arr()

# Output:
# false
# false
# false
# false
# false
# true
# true
# false
# false
# true
# true
# false
# true
# false
# true


mod ../../../../../stdlib/array_utils.wacc;

begin
	char[] arr = [] ;
	bool contains = call contains_char_arr('c', arr) ;
	println contains ;
	arr = ['c'] ;
	contains = call contains_char_arr('b', arr) ;
	println contains ;
	arr = ['[', 'a'] ;
	contains = call contains_char_arr('Y', arr) ;
	println contains ;
	arr = ['b', 'a', 'a'] ;
	contains = call contains_char_arr('Z', arr) ;
	println contains ;
	arr = ['c', 'c', 'Y', 'c'] ;
	contains = call contains_char_arr('`', arr) ;
	println contains ;
	arr = ['[', 'c', 'c', 'c', 'X'] ;
	contains = call contains_char_arr('c', arr) ;
	println contains ;
	arr = ['c', 'a', '^', '_', 'Z', 'c'] ;
	contains = call contains_char_arr('^', arr) ;
	println contains ;
	arr = ['c', 'X', 'X', 'Y', '`', 'a', 'a'] ;
	contains = call contains_char_arr('_', arr) ;
	println contains ;
	arr = ['Z', 'X', 'Z', '^', 'a', 'Z', '[', '^'] ;
	contains = call contains_char_arr('`', arr) ;
	println contains ;
	arr = ['b', ']', 'Z', 'b', 'X', ']', 'Y', '^', 'c'] ;
	contains = call contains_char_arr('Z', arr) ;
	println contains ;
	arr = ['a', 'b', 'X', 'c', 'Z', ']', 'Y', 'X', 'a', '['] ;
	contains = call contains_char_arr('[', arr) ;
	println contains ;
	arr = ['[', '`', ']', 'b', '_', 'b', 'b', ']', '_', 'c', 'Z'] ;
	contains = call contains_char_arr('Y', arr) ;
	println contains ;
	arr = ['`', '_', '[', 'Z', 'Z', 'a', 'X', 'b', 'a', '[', '`', '`'] ;
	contains = call contains_char_arr('b', arr) ;
	println contains ;
	arr = ['b', 'c', 'Y', 'a', 'c', 'a', 'Y', 'Y', 'X', 'X', 'a', 'X', '_'] ;
	contains = call contains_char_arr('[', arr) ;
	println contains ;
	arr = ['X', 'Z', ']', ']', 'a', 'b', 'Y', 'Y', '^', 'Y', 'b', '_', 'Y', 'a'] ;
	contains = call contains_char_arr('a', arr) ;
	println contains
end