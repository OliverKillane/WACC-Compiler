use super::graph_coloring::GeneralAssembly;
use std::fmt::Display;

impl Display for GeneralAssembly {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        todo!()
    }
}
